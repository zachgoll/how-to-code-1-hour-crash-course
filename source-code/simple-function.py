# This file shows how to write and call a simple Python function.

# This function will take a single argument, multiply that argument by 2, and
# then return the value
def simple_function(arg1):
    return arg1 * 2
    
# We can "call" the function and store the return value of the function in a variable.
output_to_function = simple_function(20)

